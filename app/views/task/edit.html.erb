<h1>Edit Task</h1>

<%= render partial: "form", locals: { task: @task } %>

<%= button_to "Delete", @task, method: :delete, data: { turbo_confirm: "Are you sure?" }, class: "bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline mt-4" %>
